# OpenSpec Instructions

Specs in `specs/` describe what IS built. Proposed changes go in `changes/<change-id>/`
with `proposal.md`, `tasks.md`, and spec deltas; archive them into `specs/` once deployed.
Read `project.md` for conventions before proposing changes.
